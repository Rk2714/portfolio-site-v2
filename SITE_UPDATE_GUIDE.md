# Yazirusiサイト 更新手順

この資料は、プログラミングに慣れていなくてもサイトを確認・修正・公開できるようにした手順書です。

迷ったときは、無理に先へ進まずCodexへ画面やエラーを見せてください。

## 最初に覚える3つ

1. `ローカル`は、自分のMacだけで見られる確認用サイトです。
2. `保存`しただけでは、公開中の `yazirusi.com` は変わりません。
3. GitHubの`main`へ反映すると、公開処理が始まります。公開前には必ず確認を行います。

---

## A. サイトを見るだけ

### Step 1: ターミナルを開く

Macの「ターミナル」アプリを開きます。

### Step 2: サイトのフォルダへ移動する

次の1行を貼り付けて、Enterを押します。

```bash
cd /Users/ryuyakinjo/Desktop/portfolio-site
```

### Step 3: ローカルサイトを起動する

```bash
npm run dev:local
```

`Ready`と表示されたら起動完了です。ターミナルは閉じずに、そのままにします。

### Step 4: ブラウザで見る

[http://127.0.0.1:3011/](http://127.0.0.1:3011/) を開きます。

主な確認ページ:

- トップ: [http://127.0.0.1:3011/](http://127.0.0.1:3011/)
- 料金: [http://127.0.0.1:3011/pricing](http://127.0.0.1:3011/pricing)
- ラジオ: [http://127.0.0.1:3011/radio](http://127.0.0.1:3011/radio)
- メディア: [http://127.0.0.1:3011/media](http://127.0.0.1:3011/media)

### Step 5: 終了する

起動に使ったターミナルを選び、`Control`キーを押しながら`C`を押します。

---

## B. 文章や画像を修正する

### Step 1: 先に現在の状態を確認する

```bash
cd /Users/ryuyakinjo/Desktop/portfolio-site
git status --short --branch
```

文字が表示されても、勝手に削除・元に戻す操作はしません。作業途中の変更があるという意味です。

### Step 2: Codexへ変更内容を具体的に伝える

次の形で依頼すると安全です。

```text
/Users/ryuyakinjo/Desktop/portfolio-site を修正してください。
変更したい場所: トップページの○○部分
現在の文章: 「○○」
変更後の文章: 「○○」
元の変更は消さず、公開はまだしないでください。
修正後にスマホ表示とビルドを確認してください。
```

文章が決まっていない場合:

```text
トップページの○○が分かりづらいです。
今の内容を残したまま、候補を3案出してください。
まだファイルは変更しないでください。
```

### Step 3: 画像を渡す

画像をこのチャットへ添付し、次の3点を伝えます。

- どのページで使うか
- どの位置に置くか
- トリミングしてよいか

人物写真は、本人の顔が変わる生成加工を行わず、承認済み画像を使います。

現在の主な画像フォルダ:

```text
/Users/ryuyakinjo/Desktop/portfolio-site/public/images
```

### Step 4: ブラウザで確認する

ローカルサイトが起動中なら、ブラウザを再読み込みします。

最低限、次を確認します。

- 自分の名前が「金城竜弥」になっている
- 文章の改行が不自然ではない
- ボタンを押すと正しい場所へ移動する
- スマホ幅でも文字や画像がはみ出さない
- 料金や経歴に誤解を招く表現がない

---

## C. 公開前の確認

### Step 1: 自動チェックを行う

```bash
cd /Users/ryuyakinjo/Desktop/portfolio-site
npm run check
```

最後まで進み、赤い`error`や`failed`がなければ基本確認は完了です。

分からない表示が出た場合は、その画面をCodexへ貼り付けます。自己判断でファイルを削除しません。

### Step 2: 3つの画面幅で見る

- スマホ: 375px程度
- タブレット・小型PC: 884px程度
- デスクトップ: 1440px程度

Codexへ依頼する場合:

```text
公開前チェックをしてください。
375px、884px、1440pxで、横スクロール、文字切れ、画像切れ、ボタン崩れを確認してください。
リンク切れ、見出し構造、SEO情報、ブラウザエラーも確認してください。
まだ公開はしないでください。
```

### Step 3: 変更内容を確認する

```bash
git status --short
```

意図しないファイルが含まれていないか、Codexに確認を依頼します。

---

## D. 公開する

公開はサイト全体に影響するため、最終確認後に行います。

### Step 1: Codexへ公開前レビューを依頼する

```text
この変更を公開する前にレビューしてください。
変更ファイル、表示確認、リンク、個人情報、APIキー、セキュリティ、SEOを確認し、問題があれば公開せず教えてください。
```

### Step 2: 問題がなければ公開を依頼する

```text
確認済みの変更だけをコミットしてください。
コミット内容を説明してから、yazirusi.comへ反映されるmainブランチへプッシュしてください。
関係ない変更は含めないでください。
```

`プッシュ`は外部公開につながる操作です。Codexが実行内容を説明した後に、公開してよいか判断します。

### Step 3: 公開後に確認する

[https://yazirusi.com/](https://yazirusi.com/) を開き、次を確認します。

- トップページが開く
- 料金ページが開く
- 予約ボタンが正しい予約先へ進む
- スマホでも表示が崩れない
- 古い表示の場合は数分待って再読み込みする

---

## よくある困りごと

### 3011番が開かない

別のターミナルで次を実行します。

```bash
cd /Users/ryuyakinjo/Desktop/portfolio-site
npm run dev:local
```

### `port is already in use`と表示された

すでにサイトが起動している可能性があります。まず [http://127.0.0.1:3011/](http://127.0.0.1:3011/) を開きます。

それでも見られない場合は、エラー全文をCodexへ送ります。

### 修正したのに画面が変わらない

1. ブラウザを再読み込みします。
2. 直らなければ、起動中のターミナルで`Control + C`を押します。
3. もう一度`npm run dev:local`を実行します。

### 間違えてしまった

削除や`git reset`は実行せず、次のようにCodexへ伝えます。

```text
操作を間違えた可能性があります。
現在の変更を消さずに状態を確認し、安全な戻し方を説明してください。
まだ戻す操作はしないでください。
```

---

## Codexへ毎回伝えると安全な一文

```text
既存の変更を消さず、まず現状を確認してください。公開・プッシュ・削除は、実行前に内容を説明してください。
```
